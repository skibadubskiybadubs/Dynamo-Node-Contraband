[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Default: run unit tests only (no IPC/E2E which require Revit)
# Use `pytest --run-ipc` to include IPC tests
markers =
    ipc: tests that require a running Revit instance with DynamoCliAddIn
